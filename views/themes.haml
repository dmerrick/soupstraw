.container

  .col-md-4
    %h2
      = @title

    %p
      %ul
        - @themes.each do |theme|
          %li 
            %a{href: "http://#{theme}.#{request.host_with_port + request.path_info}"}
              = theme